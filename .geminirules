# Commit & Push Restriction Rule
- AI는 코드를 작성하거나 파일을 수정한 뒤, 사용자가 명시적으로 "커밋해 줘" 또는 "푸쉬해 줘"라고 지시하기 전까지 절대로 스스로 `git commit`이나 `git push`를 실행하지 않는다.
- 작업 내역을 깃에 스테이징(`git add` 또는 `git mv` 등)하는 것까지만 허용되며, 작업이 완료되면 사용자에게 내역을 보고하고 다음 커밋/푸쉬 명령을 대기한다.
